import { DaysUntilDuePipe } from './days-until-due-pipe';

describe('DaysUntilDuePipe', () => {
  it('create an instance', () => {
    const pipe = new DaysUntilDuePipe();
    expect(pipe).toBeTruthy();
  });
});
